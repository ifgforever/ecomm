User-agent: *
Allow: /

# The admin and review tools are kept out of search by a noindex meta tag on
# each page rather than a Disallow here. A Disallow would block the crawl that
# lets Google see the noindex, which can leave a bare URL indexed anyway. The
# pages are all behind the admin login, so allowing the crawl exposes nothing.

Sitemap: https://jinkittys.com/sitemap.xml
